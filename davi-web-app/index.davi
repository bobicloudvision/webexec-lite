<davi>

session.start();

if (isset(request.session["user"])) {
    echo('<h1>Welcome, ' . htmlspecialchars(request.session["user"]) . '!</h1>');
} else {
    echo('<h1>Welcome, guest!</h1>');
    echo('<p><a href="login.davi">Login</a> or <a href="register.davi">Register</a></p>');
}

var_dump(request.session);

</davi>
